{{#each choice}}
<li>
	<label><input type="radio" name="{{../prefix}}{{@index}}" value="true">{{A}}</label>
	<label><input type="radio" name="{{../prefix}}{{@index}}" value="false">{{B}}</label>
</li>
{{/each}}
